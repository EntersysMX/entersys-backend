# 📊 CONFIGURACIÓN LOKI PARA MONITOREO SIX SIGMA
# Optimizada para logs estructurados y retención eficiente

auth_enabled: false

server:
  http_listen_port: 3100
  grpc_listen_port: 9096
  log_level: info

common:
  path_prefix: /loki
  storage:
    filesystem:
      chunks_directory: /loki/chunks
      rules_directory: /loki/rules
  replication_factor: 1
  ring:
    instance_addr: 127.0.0.1
    kvstore:
      store: inmemory

# ═══════════════════════════════════════════════════════════════
# SCHEMA CONFIGURATION - SIX SIGMA OPTIMIZED
# ═══════════════════════════════════════════════════════════════

schema_config:
  configs:
    - from: 2020-10-24
      store: boltdb-shipper
      object_store: filesystem
      schema: v11
      index:
        prefix: index_
        period: 24h

# ═══════════════════════════════════════════════════════════════
# STORAGE CONFIGURATION - RETENTION AND CLEANUP
# ═══════════════════════════════════════════════════════════════

storage_config:
  boltdb_shipper:
    active_index_directory: /loki/boltdb-shipper-active
    cache_location: /loki/boltdb-shipper-cache
    shared_store: filesystem
  filesystem:
    directory: /loki/chunks

# ═══════════════════════════════════════════════════════════════
# COMPACTOR - AUTOMATIC LOG RETENTION
# ═══════════════════════════════════════════════════════════════

compactor:
  working_directory: /loki/boltdb-shipper-compactor
  shared_store: filesystem
  compaction_interval: 10m
  retention_enabled: true
  retention_delete_delay: 2h
  retention_delete_worker_count: 150

# ═══════════════════════════════════════════════════════════════
# LIMITS CONFIGURATION - PREVENT DISK OVERFLOW
# ═══════════════════════════════════════════════════════════════

limits_config:
  # Retention per stream (30 days for Six Sigma analysis)
  retention_period: 720h  # 30 días

  # Rate limiting
  ingestion_rate_mb: 16
  ingestion_burst_size_mb: 32
  max_streams_per_user: 10000
  max_line_size: 256000

  # Query limits
  max_query_parallelism: 32
  max_query_series: 500
  max_query_range: 720h  # 30 días

  # Split queries by interval to improve performance
  split_queries_by_interval: 30m

  # Reject old samples
  reject_old_samples: true
  reject_old_samples_max_age: 72h  # 3 días

# ═══════════════════════════════════════════════════════════════
# CHUNK STORE CONFIGURATION
# ═══════════════════════════════════════════════════════════════

chunk_store_config:
  max_look_back_period: 720h  # 30 días

# ═══════════════════════════════════════════════════════════════
# TABLE MANAGER - CLEANUP OLD DATA
# ═══════════════════════════════════════════════════════════════

table_manager:
  retention_deletes_enabled: true
  retention_period: 720h  # 30 días

# ═══════════════════════════════════════════════════════════════
# QUERY RANGE CONFIGURATION
# ═══════════════════════════════════════════════════════════════

query_range:
  results_cache:
    cache:
      memcached_client:
        consistent_hash: true
        host: localhost
        service: memcached

# ═══════════════════════════════════════════════════════════════
# RULER CONFIGURATION - ALERTING ON LOGS
# ═══════════════════════════════════════════════════════════════

ruler:
  storage:
    type: local
    local:
      directory: /loki/rules
  rule_path: /loki/rules
  alertmanager_url: http://entersys-alertmanager:9093
  ring:
    kvstore:
      store: inmemory
  enable_api: true
  enable_alertmanager_v2: true

# ═══════════════════════════════════════════════════════════════
# ANALYTICS CONFIGURATION
# ═══════════════════════════════════════════════════════════════

analytics:
  reporting_enabled: false

# ═══════════════════════════════════════════════════════════════
# TRACING CONFIGURATION (OPTIONAL)
# ═══════════════════════════════════════════════════════════════

tracing:
  enabled: false